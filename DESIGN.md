# TradingSim 交易模拟器设计文档

> 版本: v0.2 | 更新日期: 2026-07-30

## 一、项目概述

TradingSim 是一个基于 Canvas 的证券交易市场模拟器，支持实时K线渲染、订单簿撮合、散户AI代理、主力资金模拟、涨跌停机制等功能。玩家通过右键绘制目标K线路径，驱动主力资金自动执行买卖操作，体验主力操盘的完整流程。

### 技术栈

- 纯前端单页应用，无后端依赖
- HTML5 Canvas 渲染K线图和订单簿深度
- 原生 JavaScript 实现交易引擎和模拟逻辑
- 零外部依赖，单文件部署

---

## 二、核心架构

```
┌─────────────────────────────────────────────────┐
│                  UIController                    │
│  (账户面板 / 委托下单 / 五档行情 / KPI)           │
├─────────────┬───────────────────┬───────────────┤
│  KLineChart │  MarketSimulator  │  ForumNews    │
│  (K线渲染)   │  (模拟引擎)       │  (舆情系统)   │
├─────────────┼───────────────────┤               │
│             │  OrderBook        │               │
│             │  (订单簿撮合)      │               │
│             ├───────────────────┤               │
│             │  PlayerAccount    │               │
│             │  (玩家账户)        │               │
│             ├───────────────────┤               │
│             │  RetailAgent[]    │               │
│             │  (散户AI代理)      │               │
│             ├───────────────────┤               │
│             │  KLineAggregator  │               │
│             │  (K线聚合器)       │               │
│             ├───────────────────┤               │
│             │  KPITracker       │               │
│             │  (KPI追踪器)       │               │
└─────────────┴───────────────────┴───────────────┘
```

### 模块职责

| 模块 | 职责 |
|------|------|
| `OrderBook` | 限价单/市价单撮合，维护买卖盘深度，涨跌停价格限制 |
| `MarketSimulator` | 模拟引擎主循环，协调散户下单、主力执行、玩家委托、价格更新 |
| `PlayerAccount` | 玩家账户管理，资金/持仓/T+1结算/订单状态 |
| `RetailAgent` | 散户AI代理，4种策略（动量/逆向/价值/随机） |
| `KLineChart` | Canvas K线图渲染，支持分时/日K/周K/月K，右键绘制路径 |
| `KLineAggregator` | 逐笔成交聚合为5分钟K线 |
| `KPITracker` | 年化收益率、最大回撤、夏普比率等KPI计算 |
| `UIController` | DOM更新，账户面板、委托表单、行情展示 |

---

## 三、交易引擎设计

### 3.1 订单簿撮合

采用价格优先、时间优先的连续竞价机制：

```
addLimitOrder(side, price, qty):
  1. 涨跌停价格限制：price = clamp(price, limitDown, limitUp)
  2. 封板检查：涨停封板时拒绝高价卖单，跌停封板时拒绝低价买单
  3. 撮合阶段：
     - 买单：从最低卖价开始向上匹配
     - 卖单：从最高买价开始向下匹配
  4. 剩余量挂入订单簿
```

### 3.2 订单簿深度（v0.2 优化）

初始挂单由 `seed()` 方法生成，模拟做市商提供的流动性：

| 参数 | v0.1（旧） | v0.2（新） | 说明 |
|------|-----------|-----------|------|
| 档位数 | 20档 | 15档 | 减少远端流动性 |
| 档位间距 | 5 tick (0.05) | 2 tick (0.02) | 更紧凑的价差 |
| 每档挂单量 | 500-2500手 | 50-300手 | 大幅降低，使大单能推动价格 |
| 距离衰减 | 无 | 线性衰减(0.95^n) | 远端更薄 |

**设计原因**：v0.1 的订单簿过深（总卖盘约300万股），主力买入只能吃掉1-2档，价格几乎不动。v0.2 降至每档5千-3万股，15档总计约20万股，主力单笔2万股的买单可以吃穿多档，产生明显的价格冲击。

### 3.3 涨跌停机制（v0.2 新增）

```
setPriceLimits(prevClose):
  limitUp   = prevClose * 1.10  (涨停价 +10%)
  limitDown = prevClose * 0.90  (跌停价 -10%)

updateLimitStatus():  // 每个 tick 调用
  涨停封板条件: currentPrice >= limitUp && 买盘在涨停价附近 && 卖盘耗尽
  跌停封板条件: currentPrice <= limitDown && 卖盘在跌停价附近 && 买盘耗尽
  封板解除: 价格回落 或 对手盘出现
```

封板时：
- 涨停封板：阻止高于涨停价的卖单进入
- 跌停封板：阻止低于跌停价的买单进入
- 触发 Toast 提示「涨停封板」/「跌停封板」/「涨停打开」/「跌停打开」

### 3.4 价格更新策略（v0.2 优化）

| 版本 | 公式 | 问题 |
|------|------|------|
| v0.1 | `currentPrice = midPrice` | 中间价抹平波动，即使大单吃穿多档，中间价变化也很小 |
| v0.2 | `currentPrice = lastTrade * 0.7 + midPrice * 0.3` | 70%权重给最后成交价，放大交易冲击，30%权重给中间价保持稳定性 |

最终价格还会被涨跌停限制 clamp，确保不超出当日范围。

---

## 四、主力资金模拟

### 4.1 绘制路径执行

玩家通过右键拖拽在分时图上绘制目标价格路径，系统将其转化为时间序列目标点：

```
executeDrawPath():
  1. 查找当前时间对应的最近目标点
  2. 计算目标价与当前价的偏差
  3. 偏差 > 0.2%阈值: 买入推高
     偏差 < -0.2%阈值: 卖出压低
  4. 下单量 = maxAmmo * 0.02 * strength（v0.2从0.003提升至0.02）
  5. 限制：不超过可用筹码、不超过可用资金、卖出不超过可用持仓(T+1)
```

### 4.2 下单量参数对比

| 参数 | v0.1 | v0.2 | 影响 |
|------|------|------|------|
| 下单系数 | 0.003 | 0.02 | 单笔最大从3,000股提升至20,000股 |
| 偏差阈值 | 0.2% | 0.2% | 不变 |
| 强度归一化 | 2% | 2% | 不变 |

### 4.3 无对手盘处理（v0.2 新增）

当卖盘被吃光（bestAsk=0）时，v0.1 的机器买单会挂在当前价无法成交。v0.2 改进：

1. 优先使用市价单扫货（`addMarketOrder`）
2. 若市价单也无成交，在当前价上方 0.1% 挂买单刺激卖方入场

---

## 五、散户AI代理

### 5.1 代理策略

| 类型 | 策略 | 下单方式 |
|------|------|---------|
| momentum | 追涨杀跌：近期均价上涨则买，下跌则卖 | 对手价 +/- 随机tick |
| contrarian | 逆向操作：近期均价下跌则买，上涨则卖 | 对手价 +/- 随机tick |
| value | 价值投资：低于感知价值则买，高于则卖 | 对手价 +/- 随机tick |
| random | 随机买卖 | 对手价 +/- 随机tick |

### 5.2 单边订单簿处理（v0.2 修复）

v0.1 中当 `bestBid=0` 或 `bestAsk=0` 时代理直接返回 null，导致卖盘耗尽后交易完全停滞。v0.2 改为使用 `currentPrice` 作为缺失一侧的参考价：

```javascript
const refBid = bb > 0 ? bb : currentPrice;
const refAsk = ba > 0 ? ba : currentPrice;
```

---

## 六、账户与风控

### 6.1 资金安全（v0.2 修复）

**问题**：v0.1 的 `onFill()` 在资金检查前就增加了 `filledQty`，当资金不足提前返回时未回退，导致订单状态错误；Agent 类的 `onFill()` 完全没有资金保护。

**修复方案**：

```
PlayerAccount.onFill(order, fillPrice, fillQty):
  买入:
    1. 计算最大可买量 maxAffordable = floor(cash / fillPrice)
    2. actualFillQty = min(fillQty, maxAffordable)
    3. 若 actualFillQty <= 0: 取消订单，返回
    4. 仅对 actualFillQty 更新 filledQty 和 avgFillPrice
    5. 扣减资金，增加持仓（T+1冻结）
  卖出:
    1. 计算最大可卖量 = pos.available
    2. actualFillQty = min(fillQty, sellable)
    3. 若 actualFillQty <= 0: 取消订单，返回
    4. 仅对 actualFillQty 更新 filledQty 和 avgFillPrice
    5. 增加资金，减少持仓

  安全阀: if (cash < 0) cash = 0
```

### 6.2 T+1 结算

买入当日冻结，次日开盘解冻为可用：

```
settleT1():
  pos.available += pos.frozen
  pos.frozen = 0
```

### 6.3 机器订单资金限制

主力买入前检查可用资金，限制下单量：

```javascript
const maxByCash = Math.floor(account.cash / buyPrice / lotSize) * lotSize;
orderQty = Math.min(orderQty, maxByCash);
```

---

## 七、K线图渲染

### 7.1 绘制路径（v0.2 修复）

**问题**：v0.1 的 `drawPath()` 从 `pt.price` 重新计算 y 坐标，使用当前 `priceRange`。模拟运行时价格范围变化，导致绘制点偏离鼠标位置。

**修复**：直接使用 `pt.x` 和 `pt.y` 原始鼠标坐标渲染，不再从价格重新计算：

```javascript
drawPath(ctx):
  for pt in drawPoints:
    moveTo/lineTo(pt.x, pt.y)  // 直接使用原始坐标
```

### 7.2 图表交互

| 操作 | 功能 |
|------|------|
| 左键拖动 | 平移浏览历史K线 |
| 右键拖动 | 绘制目标价格路径（仅分时图） |
| 滚轮 | 缩放K线宽度 |
| 自动滚动 | 跟踪最新K线 |

---

## 八、配置参数

```javascript
CONFIG = {
  initialCash:   10000000,   // 初始资金 1000万
  initialAmmo:   1000000,    // 主力筹码 100万股
  basePrice:     10.00,      // 基准价格
  priceTick:     0.01,       // 最小变动价位
  lotSize:       100,        // 1手 = 100股
  maxPriceChange: 0.10,      // 单日最大涨跌幅 10%
  tradingHours:  240,        // 交易时间 240分钟
  retailAgentCount: 50,      // 散户数量
  retailOrderRate: 0.6,      // 散户下单概率
}
```

---

## 九、v0.1 → v0.2 变更总结

### Bug 修复

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 右键绘制点不在鼠标下面 | drawPath 从 price 重新计算 y 坐标，价格范围变化导致偏移 | 直接使用 pt.x/pt.y 原始鼠标坐标 |
| 2 | 可用资金可以为负 | onFill 提前返回时 filledQty 未回退；Agent 无资金保护 | 重构 onFill 先确定实际成交量再更新；Agent 增加资金 clamp |

### 算法优化

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 3 | 大笔买入无法涨停封板 | 订单簿深度过深（300万股卖盘），大单无法推动价格 | 每档降至5千-3万股，15档，距离衰减 |
| 4 | 机器下单量过小 | 系数0.003，单笔仅3000股 | 提升至0.02，单笔最大20000股 |
| 5 | 没有涨跌停机制 | maxPriceChange 定义但未使用 | 实现涨停/跌停价格限制和封板/打开检测 |
| 6 | 波动率过低 | 价格更新用中间价抹平波动 | 改为70%最后成交价 + 30%中间价 |
| 7 | 卖盘耗尽后交易停滞 | 代理在单边订单簿时直接返回null | 用currentPrice作为缺失一侧参考价 |
| 8 | 机器无对手盘时卡死 | 无卖盘时买单挂在当前价无法成交 | 使用市价单扫货 + 高价买单刺激 |

### 验证结果

涨停封板测试（目标价 = 涨停价 11.00）：

| Tick | 价格 | 涨幅 | 状态 |
|------|------|------|------|
| 10 | 10.16 | +1.6% | 上涨中 |
| 20 | 10.31 | +3.1% | 上涨中 |
| 30 | 10.52 | +5.2% | 上涨中 |
| 40 | 10.80 | +8.0% | 上涨中 |
| 50 | 11.00 | +10.0% | 涨停封板 |
| 60-110 | 11.00 | +10.0% | 持续封板 |
| 120 | 10.98 | +9.8% | 封板打开 |

资金安全测试：

| 场景 | 结果 |
|------|------|
| 5次快速填充耗尽资金 | 最后订单正确取消，资金=0，从未为负 |
| 高价填充（15元 vs 10元） | 自动减少填充量，资金非负 |
| 零资金时尝试买入 | 订单取消，资金不变 |
| 100 tick 激进买入 | 资金 1000万→858万，始终非负 |

---

## 十、文件结构

```
trading-simulator-demo/
├── index.html      # 单文件应用，包含全部HTML/CSS/JS
└── DESIGN.md       # 本设计文档
```

---

## 十一、未来计划

- [ ] 支持多标的切换时的持仓独立管理
- [ ] 增加大单拆分（TWAP/VWAP）执行算法
- [ ] 添加集合竞价阶段
- [ ] 散户代理增加跟风/恐慌情绪模型
- [ ] K线图增加技术指标（MA/MACD/KDJ）
- [ ] 支持融资融券（做空机制）
